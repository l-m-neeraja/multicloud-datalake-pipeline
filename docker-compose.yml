version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: multicloud-datalake-pipeline:latest
    env_file:
      - .env
    working_dir: /app
    volumes:
      - ./:/app
